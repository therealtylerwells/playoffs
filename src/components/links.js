import React from 'react';

const links = (props) => (
  <div style={{'flex':'1'}}>
    <div style={{'display':'flex'}}>
      <span>Tickets</span>
      <span style={{'flex':'1'}}></span>
      <span>Preview</span>
    </div>
  </div>
)

export default links;